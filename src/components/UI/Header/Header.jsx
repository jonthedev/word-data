import { BackgroundImg } from "../BackgroundImg/BackgroundImg"
import { Title } from "../../Typography/Title/Title"

export const Header = () => {
  return (
    <header>
      <BackgroundImg />
      <Title size="h1" />
    </header>
  )
}
